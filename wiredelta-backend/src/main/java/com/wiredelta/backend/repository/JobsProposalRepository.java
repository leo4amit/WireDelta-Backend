package com.wiredelta.backend.repository;

import com.wiredelta.backend.entity.JobsProposal;
import org.springframework.data.jpa.repository.JpaRepository;

public interface JobsProposalRepository extends JpaRepository<JobsProposal,Long> {
}
